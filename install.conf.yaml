- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#   ~/.dotfiles: ''
    ~/.xinitrc: xinitrc
    ~/.zshrc: zshrc
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.Xresources: Xresources

- shell:
  - [git submodule update --init --recursive, Installing submodules]
